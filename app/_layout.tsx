import { Stack } from 'expo-router';

export default function Layout() {
  return (
    <Stack>
      {/* You can add screen options here that apply to all screens in the stack */}
      {/* For example: */}
      {/* <Stack.Screen name="index" options={{ title: 'Home' }} /> */}
    </Stack>
  );
}
